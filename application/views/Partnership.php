<!-- WRAPPER -->
	<title>Kemitraan </title>
		<div id="wrapper">
			<!-- PAGE TITLE -->
			<div class="container">
				<div class="row">
                	<!-- Profil Perusahaan -->
                	<hr style="margin-top: -30px;">
                    <div class="col-md-8" style="margin-top: 10px;">
                    <h1 style="margin-top: 20px; color: black; font-size: 1.5em;" ><b>Kemitraan</b></h1>
                    
                    <b>Peluang usaha kemitraan keagenan dengan syarat: </b><br>
                    <b>Syarat Administrasi </b><br>
                    <li>Mengisi surat permohonan keagenan sales counter . </li>
                    <li>Pas foto berwarna untuk penanggung jawab dengan ukuran 4 x 6 (3 lembar). </li>
                    <li>Fotokopi NPWP, SIUP, Akte Pendirian perusahaan (bila ada). </li>
                    <li>Membayar biaya administrasi keagenan (untuk izin, spanduk, brosur, peralatan pendukung, dll) </li>
                    <li>Menandatangani nota kesepahaman MOU yang merupakan perjanjian kerja sama awal. </li>
                    
                    <b>Syarat Lokasi</b><br>
                    <li>Denah lokasi kios / counter. </li>
                    <li>Dapat diakses dengan kendaraan roda 4. </li>
                    <li>Lokasi kios / counter harus lulus survey oleh tim survei. </li>
                    
                    <b>Syarat SDM</b><br>
                    <li>Memiliki petugas dengan minimal pendidikan SMA / SMK. </li>
                    <li>Wajib menggunakan seragam. </li>
                    <li>Menjaga nama baik perusahaan. </li>
                    
                    <b>Syarat E-System</b><br>
                    <li>Memiliki 1 unit komputer / laptop / printer. </li>
                    <li>Memiliki 1 unit HP Android. </li>
                    <li>Memiliki jaringan internet. </li>
                    <li>Memiliki 1 unit timbangan bertera dengan berat minimal 100 kg. </li><br>
                    
                    <p>*Syarat dan Ketentuan Berlaku.<br>
                    Hubungi Team Suport kami untuk lebih jelasnya.</p>
                    </div>
                    
                            
<style>
/* Import Google font - Poppins */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap');
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}
bodyqu {
  display: flex;
  padding: 0 1px;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(to left top, #f9f5f5, #e8e5e5);
}

body-taxt {
  display: flex;
  padding: 0 1px;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(to left top, #031A9A, #8B53FF);
}


.wrapper {
  max-width: 1100px;
  width: 100%;
  position: relative;
}
.wrapper i {
  top: 50%;
  height: 50px;
  width: 50px;
  cursor: pointer;
  font-size: 1.25rem;
  position: absolute;
  text-align: center;
  line-height: 50px;
  background: #fff;
  border-radius: 50%;
  box-shadow: 0 3px 6px rgba(0,0,0,0.23);
  transform: translateY(-50%);
  transition: transform 0.1s linear;
}
.wrapper i:active{
  transform: translateY(-50%) scale(0.85);
}
.wrapper i:first-child{
  left: -22px;
}
.wrapper i:last-child{
  right: -22px;
}
.wrapper .carousel{
  display: grid;
  grid-auto-flow: column;
  grid-auto-columns: calc((100% / 3) - 12px);
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  gap: 16px;
  border-radius: 8px;
  scroll-behavior: smooth;
  scrollbar-width: none;
}
.carousel::-webkit-scrollbar {
  display: none;
}
.carousel.no-transition {
  scroll-behavior: auto;
}
.carousel.dragging {
  scroll-snap-type: none;
  scroll-behavior: auto;
}
.carousel.dragging .card {
  cursor: grab;
  user-select: none;
}
.carousel :where(.card, .img) {
  display: flex;
  justify-content: center;
  align-items: center;
}
.carousel .card {
  scroll-snap-align: start;
  height: 600px;
  list-style: none;
  /*background: #fff;*/
  background: linear-gradient(to left top, #f8f9f9 , #eae7e7);
  /*background-image: url(https://tripcargoid.com//assets/upload/BG_Profil.jpg);*/
  cursor: pointer;
  padding-bottom: 10px;
  padding-top: 5px;
  flex-direction: column;
  border-radius: 8px;
  width: 100% !important;
}
.carousel .card .img {
  margin-top: 100px;  
  background: #8B53FF;
  height: 148px;
  width: 148px;
  border-radius: 50%;
}
.card .img img {
  width: 140px;
  height: 140px;
  border-radius: 50%;
  object-fit: cover;
  border: 4px solid #ddd;
}
.carousel .card h2 {
  font-weight: 500;
  font-size: 1.2em;
  margin: 5px 0px 5px 0px;
}
.carousel .card span {
  color: #6A6D78;
  font-size: 1.31rem;
}

@media screen and (max-width: 900px) {
  .wrapper .carousel {
    grid-auto-columns: calc((100% / 2) - 9px);
  }
}

@media screen and (max-width: 600px) {
  .wrapper .carousel {
    grid-auto-columns: 100%;
  }
}
</style> 


                    <div class="col-md-4">
                        <hr>
	                        <div class="row" style="background-color: #fff">
                            <ul class="carousel">
                                <li class="card">
                                <div class="img">
                                    <img src="https://tripcargoid.com/assets/atropos/images/deretan usaha masa depan.webp" alt="kemitraaan" style="max-width: 540px; height: auto; margin: 20.5px 0px; width: 300px;">
                                </div>
                                <p><a href="https://tripcargoid.com/join"><b>Ayo! bergabung bersama Trip Cargo</b></a>
                                <br><br>
                                Kembangkan bisnismu sekarang juga<br>
                                Bisnis logistik adalah bisnis cerdas
                                </p>
                                </li>
                            </ul>
                            </div>
                    </div> 
   
<script type="text/javascript">
const wrapper = document.querySelector(".wrapper");
const carousel = document.querySelector(".carousel");
const firstCardWidth = carousel.querySelector(".card").offsetWidth;
const arrowBtns = document.querySelectorAll(".wrapper i");
const carouselChildrens = [...carousel.children];

let isDragging = false, isAutoPlay = true, startX, startScrollLeft, timeoutId;

// Get the number of cards that can fit in the carousel at once
let cardPerView = Math.round(carousel.offsetWidth / firstCardWidth);

// Insert copies of the last few cards to beginning of carousel for infinite scrolling
carouselChildrens.slice(-cardPerView).reverse().forEach(card => {
    carousel.insertAdjacentHTML("afterbegin", card.outerHTML);
});

// Insert copies of the first few cards to end of carousel for infinite scrolling
carouselChildrens.slice(0, cardPerView).forEach(card => {
    carousel.insertAdjacentHTML("beforeend", card.outerHTML);
});

// Scroll the carousel at appropriate postition to hide first few duplicate cards on Firefox
carousel.classList.add("no-transition");
carousel.scrollLeft = carousel.offsetWidth;
carousel.classList.remove("no-transition");

// Add event listeners for the arrow buttons to scroll the carousel left and right
arrowBtns.forEach(btn => {
    btn.addEventListener("click", () => {
        carousel.scrollLeft += btn.id == "left" ? -firstCardWidth : firstCardWidth;
    });
});

const dragStart = (e) => {
    isDragging = true;
    carousel.classList.add("dragging");
    // Records the initial cursor and scroll position of the carousel
    startX = e.pageX;
    startScrollLeft = carousel.scrollLeft;
}

const dragging = (e) => {
    if(!isDragging) return; // if isDragging is false return from here
    // Updates the scroll position of the carousel based on the cursor movement
    carousel.scrollLeft = startScrollLeft - (e.pageX - startX);
}

const dragStop = () => {
    isDragging = false;
    carousel.classList.remove("dragging");
}

const infiniteScroll = () => {
    // If the carousel is at the beginning, scroll to the end
    if(carousel.scrollLeft === 0) {
        carousel.classList.add("no-transition");
        carousel.scrollLeft = carousel.scrollWidth - (2 * carousel.offsetWidth);
        carousel.classList.remove("no-transition");
    }
    // If the carousel is at the end, scroll to the beginning
    else if(Math.ceil(carousel.scrollLeft) === carousel.scrollWidth - carousel.offsetWidth) {
        carousel.classList.add("no-transition");
        carousel.scrollLeft = carousel.offsetWidth;
        carousel.classList.remove("no-transition");
    }

    // Clear existing timeout & start autoplay if mouse is not hovering over carousel
    clearTimeout(timeoutId);
    if(!wrapper.matches(":hover")) autoPlay();
}

const autoPlay = () => {
    if(window.innerWidth < 800 || !isAutoPlay) return; // Return if window is smaller than 800 or isAutoPlay is false
    // Autoplay the carousel after every 2500 ms
    timeoutId = setTimeout(() => carousel.scrollLeft += firstCardWidth, 2500);
}
autoPlay();

carousel.addEventListener("mousedown", dragStart);
carousel.addEventListener("mousemove", dragging);
document.addEventListener("mouseup", dragStop);
carousel.addEventListener("scroll", infiniteScroll);
wrapper.addEventListener("mouseenter", () => clearTimeout(timeoutId));
wrapper.addEventListener("mouseleave", autoPlay);
</script>
</div>                        
                        
                    </div>
                </div>
		    </div>
	    </div>
		
	<style>
	    .p {color: red; text-align:center ;font-size: 1.0em; line-height:25px}
	    .h1 {color: black; text-align:left ;font-size: 1.0em; line-height:25px}
	    .col-md-8 {color: black; text-align:left ;font-size: 1.0em; line-height:25px}
	    .col-md-8 p{color: black; text-align:left ;font-size: 1.0em; line-height:25px}
	    .col-md-8 li{margin-left:0px;color: black; text-align:left ;font-size: 1.2em; line-height:25px}
	    .col-md-8 b{color: black; text-align:left ;font-size: 1.2em; line-height:25px}
	    .col-md-8 p1{border-left:3px; color: red; text-align:left ;font-size: 1.2em; line-height:25px}
	    .col-md-4 {color: black; text-align:left ;font-size: 1.0em; line-height:25px}
	    .col-md-4 p{margin:22% !important; color: #101E6F; text-align:center ;font-size: 1.5em; line-height:25px}
	    .col-md-4 li{margin-left:0px;color: black; text-align:left ;font-size: 1.0em; line-height:25px}
		.col-md-6 li{border-left:0px solid #cc3d3d;padding-left: 10px;list-style:none;margin-bottom:5px;style='text-align:justify}
	</style>
